# ルビ（振り仮名）表示 実装プラン

> 過去の設計・計画または記録です。現在の仕様や実装完了を示すものではありません。
> 保存元: `docs/superpowers/plans/2026-04-16-ruby-text.md`（v0.7.3時点）。


> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** EPUBの `<ruby><rt>` タグをパースし、横書き（上側）・縦書き（右側）でルビテキストを小フォントで描画する。

**Architecture:** パーサー → データ構造 → レイアウト → レンダリング → キャッシュの5層を順に変更する。ルビテキストは `ParsedText` / `TextBlock` の並列ベクタ `rubyTexts` で管理し、パース時にCJK文字単位で均等分割することで改行処理との整合性を保つ。

**Tech Stack:** C++20, ESP32-C3 (PlatformIO), expat XMLパーサー, SdFat

**Spec:** `docs/superpowers/specs/2026-04-16-ruby-text-design.md`

---

## ファイル構成

| ファイル | 操作 | 責務 |
|---------|------|------|
| `lib/Epub/Epub/ParsedText.h` | 変更 | `rubyTexts` ベクタ追加、`addWord` にルビ引数追加 |
| `lib/Epub/Epub/ParsedText.cpp` | 変更 | `addWord`/`extractLine`/`emitColumn`/消費処理でルビ伝搬 |
| `lib/Epub/Epub/blocks/TextBlock.h` | 変更 | `rubyTexts` ベクタ追加、コンストラクタ拡張、`hasRuby()` |
| `lib/Epub/Epub/blocks/TextBlock.cpp` | 変更 | ルビ描画 + serialize/deserialize 拡張 |
| `lib/Epub/Epub/parsers/ChapterHtmlSlimParser.h` | 変更 | ルビ状態変数追加 |
| `lib/Epub/Epub/parsers/ChapterHtmlSlimParser.cpp` | 変更 | SKIP_TAGS修正 + ruby/rtタグ処理 + ルビ均等分割 + `addLineToPage` ルビ行高さ考慮 |
| `lib/Epub/Epub/Section.cpp:15` | 変更 | `SECTION_FILE_VERSION` インクリメント |

---

### Task 1: ParsedText にルビデータ構造を追加

**Files:**
- Modify: `lib/Epub/Epub/ParsedText.h:17-50`
- Modify: `lib/Epub/Epub/ParsedText.cpp:108-139,206-216,288-306,349-354,652-663`

- [ ] **Step 1: ParsedText.h に `rubyTexts` ベクタとメソッド変更を追加**

`lib/Epub/Epub/ParsedText.h` の private セクションに `rubyTexts` を追加し、`addWord` のオーバーロードにルビ引数を追加:

```cpp
// 行 17-20 の既存ベクタの後に追加:
  std::vector<std::string> rubyTexts;  // words と並列、ルビなしは空文字列
```

`addWord` に新しいオーバーロード（行48-50 の後に追加）:

```cpp
  void setRubyForLastWord(const std::string& ruby);
```

- [ ] **Step 2: ParsedText.cpp の `addWord` で `rubyTexts` を同期**

`lib/Epub/Epub/ParsedText.cpp` の `addWord()` メソッド (行108-129)。`words.reserve(800)` ブロック内に `rubyTexts.reserve(800)` を追加し、`wordStyles.push_back` の後に `rubyTexts.push_back("")` を追加:

```cpp
// 行116-120 の reserve ブロック:
  if (words.capacity() == 0) {
    words.reserve(800);
    wordStyles.reserve(800);
    wordContinues.reserve(800);
    rubyTexts.reserve(800);  // ← 追加
  }

// 行128 の後に追加:
  rubyTexts.push_back("");
```

`setRubyForLastWord` メソッドの実装を追加:

```cpp
void ParsedText::setRubyForLastWord(const std::string& ruby) {
  if (!rubyTexts.empty()) {
    rubyTexts.back() = ruby;
  }
}
```

- [ ] **Step 3: `extractLine()` でルビを TextBlock に伝搬**

`lib/Epub/Epub/ParsedText.cpp` の `extractLine()` (行652-663)。`lineWordStyles` の後に `lineRubyTexts` を作成し、TextBlock コンストラクタに渡す:

```cpp
// 行654 の後に追加:
  std::vector<std::string> lineRubyTexts(rubyTexts.begin() + lastBreakAt, rubyTexts.begin() + lineBreak);

// 行662-663 を変更:
  processLine(
      std::make_shared<TextBlock>(std::move(lineWords), std::move(lineXPos), std::move(lineWordStyles), blockStyle,
                                  std::vector<int16_t>{}, false, std::move(lineRubyTexts)));
```

- [ ] **Step 4: `emitColumn()` でルビを TextBlock に伝搬**

`lib/Epub/Epub/ParsedText.cpp` の `emitColumn` ラムダ (行288-306)。rubyTexts を抽出して TextBlock に渡す:

```cpp
// 行293 の後に追加:
    std::vector<std::string> colRubyTexts(rubyTexts.begin() + start, rubyTexts.begin() + end);

// 行304-305 を変更:
    processColumn(std::make_shared<TextBlock>(std::move(colWords), std::move(colXpos), std::move(colStyles), blockStyle,
                                              std::move(colYpos), true, std::move(colRubyTexts)));
```

- [ ] **Step 5: 消費処理で `rubyTexts` も消去**

`layoutAndExtractLines()` の消費処理 (行206-216) に追加:

```cpp
// 行211 の後に追加:
    if (!rubyTexts.empty()) {
      const size_t rtConsumed = std::min(consumed, rubyTexts.size());
      rubyTexts.erase(rubyTexts.begin(), rubyTexts.begin() + rtConsumed);
    }
```

`layoutVerticalColumns()` の消費処理 (行349-354) に追加:

```cpp
// 行353 の後に追加:
  rubyTexts.clear();
```

- [ ] **Step 6: ビルド確認**

Run: `pio run 2>&1 | tail -5`
Expected: コンパイルエラー（TextBlock コンストラクタ不一致） — Task 2 で修正

- [ ] **Step 7: コミット**

```bash
git add lib/Epub/Epub/ParsedText.h lib/Epub/Epub/ParsedText.cpp
git commit -m "✨ ParsedText にルビデータ構造を追加"
```

---

### Task 2: TextBlock にルビデータ構造と描画を追加

**Files:**
- Modify: `lib/Epub/Epub/blocks/TextBlock.h:14-46`
- Modify: `lib/Epub/Epub/blocks/TextBlock.cpp:36-127,129-220`

- [ ] **Step 1: TextBlock.h に `rubyTexts` と `hasRuby()` を追加**

`lib/Epub/Epub/blocks/TextBlock.h`:

private メンバ（行21の後）に追加:

```cpp
  std::vector<std::string> rubyTexts;  // words と並列、ルビなしは空文字列
```

コンストラクタ（行24-32）にルビ引数を追加:

```cpp
  explicit TextBlock(std::vector<std::string> words, std::vector<int16_t> word_xpos,
                     std::vector<EpdFontFamily::Style> word_styles, const BlockStyle& blockStyle = BlockStyle(),
                     std::vector<int16_t> word_ypos = {}, bool vertical = false,
                     std::vector<std::string> ruby_texts = {})
      : words(std::move(words)),
        wordXpos(std::move(word_xpos)),
        wordStyles(std::move(word_styles)),
        blockStyle(blockStyle),
        wordYpos(std::move(word_ypos)),
        isVertical(vertical),
        rubyTexts(std::move(ruby_texts)) {}
```

public に `hasRuby()` と `getRubyTexts()` を追加:

```cpp
  bool hasRuby() const;
  const std::vector<std::string>& getRubyTexts() const { return rubyTexts; }
```

- [ ] **Step 2: TextBlock.cpp に `hasRuby()` 実装を追加**

```cpp
bool TextBlock::hasRuby() const {
  for (const auto& rt : rubyTexts) {
    if (!rt.empty()) return true;
  }
  return false;
}
```

- [ ] **Step 3: `render()` に横書きルビ描画を追加**

`lib/Epub/Epub/blocks/TextBlock.cpp` の `render()` メソッド内。横書きパスの単語描画（行92-93）の後に:

```cpp
      // 横書きルビ描画
      if (i < rubyTexts.size() && !rubyTexts[i].empty()) {
        const int rubyFontId = SMALL_FONT_ID;  // TODO: ルビ専用フォント選択
        const int baseWidth = renderer.getTextAdvanceX(effectiveFontId, words[i].c_str(), currentStyle);
        const int rubyWidth = renderer.getTextWidth(rubyFontId, rubyTexts[i].c_str(), EpdFontFamily::REGULAR);
        const int rubyX = wordX + (baseWidth - rubyWidth) / 2;
        const int rubyY = y - renderer.getLineHeight(rubyFontId) - 1;
        renderer.drawText(rubyFontId, rubyX, rubyY, rubyTexts[i].c_str(), true, EpdFontFamily::REGULAR);
      }
```

- [ ] **Step 4: `render()` に縦書きルビ描画を追加**

縦書きパス内（行69-70 の `drawTextVertical` の後）に:

```cpp
        // 縦書きルビ描画（親文字の右側）
        if (i < rubyTexts.size() && !rubyTexts[i].empty()) {
          const int rubyFontId = SMALL_FONT_ID;
          const int rubyX = wx + columnWidth + 2;
          renderer.drawTextVertical(rubyFontId, rubyX, wy, rubyTexts[i].c_str(), true, EpdFontFamily::REGULAR);
        }
```

- [ ] **Step 5: `serialize()` にルビ書き込みを追加**

`lib/Epub/Epub/blocks/TextBlock.cpp` の `serialize()` (行159-163 の後、`return true` の前)に:

```cpp
  // Ruby text data
  for (size_t i = 0; i < words.size(); i++) {
    serialization::writeString(file, (i < rubyTexts.size()) ? rubyTexts[i] : std::string());
  }
```

- [ ] **Step 6: `deserialize()` にルビ読み込みを追加**

`lib/Epub/Epub/blocks/TextBlock.cpp` の `deserialize()` (行215の後、return文の前)に:

```cpp
  // Ruby text data
  std::vector<std::string> rubyTexts(wc);
  for (auto& rt : rubyTexts) serialization::readString(file, rt);
```

TextBlock コンストラクタ呼び出し (行218-219) を変更:

```cpp
  return std::unique_ptr<TextBlock>(new TextBlock(std::move(words), std::move(wordXpos), std::move(wordStyles),
                                                  blockStyle, std::move(wordYpos), vertical, std::move(rubyTexts)));
```

- [ ] **Step 7: `fontIds.h` のインクルードを TextBlock.cpp に追加**

`SMALL_FONT_ID` を使用するため:

```cpp
#include "fontIds.h"
```

注: `fontIds.h` は `src/fontIds.h` にあるが、PlatformIO の `src/` はインクルードパスに含まれる。

- [ ] **Step 8: ビルド確認**

Run: `pio run 2>&1 | tail -5`
Expected: SUCCESS（パーサーはまだ変更前なので、ルビは表示されないが既存動作に影響なし）

- [ ] **Step 9: コミット**

```bash
git add lib/Epub/Epub/blocks/TextBlock.h lib/Epub/Epub/blocks/TextBlock.cpp
git commit -m "✨ TextBlock にルビ描画・シリアライズを追加"
```

---

### Task 3: HTMLパーサーでルビタグを処理

**Files:**
- Modify: `lib/Epub/Epub/parsers/ChapterHtmlSlimParser.h:25-104`
- Modify: `lib/Epub/Epub/parsers/ChapterHtmlSlimParser.cpp:40-44,573-578,801-982,1000-1098`

- [ ] **Step 1: ChapterHtmlSlimParser.h にルビ状態変数を追加**

private メンバに追加（例: 行42の後）:

```cpp
  // Ruby text state
  bool inRuby = false;
  int rubyStartWordIndex = -1;
  bool collectingRubyText = false;
  std::string rubyTextBuffer;
```

- [ ] **Step 2: SKIP_TAGS から "rt" を削除**

`lib/Epub/Epub/parsers/ChapterHtmlSlimParser.cpp` 行40:

```cpp
// 変更前:
const char* SKIP_TAGS[] = {"head", "rt", "rp"};
constexpr int NUM_SKIP_TAGS = sizeof(SKIP_TAGS) / sizeof(SKIP_TAGS[0]);

// 変更後:
const char* SKIP_TAGS[] = {"head", "rp"};
constexpr int NUM_SKIP_TAGS = sizeof(SKIP_TAGS) / sizeof(SKIP_TAGS[0]);
```

- [ ] **Step 3: `startElement()` にruby/rtタグの処理を追加**

SKIP_TAGSチェック（行573）の直前に追加:

```cpp
  // Ruby tag handling
  if (strcmp(name, "ruby") == 0) {
    self->flushPartWordBuffer();
    self->inRuby = true;
    self->rubyStartWordIndex =
        self->currentTextBlock ? static_cast<int>(self->currentTextBlock->size()) : 0;
    self->rubyTextBuffer.clear();
    self->depth += 1;
    return;
  }
  if (strcmp(name, "rt") == 0) {
    self->flushPartWordBuffer();
    self->collectingRubyText = true;
    self->depth += 1;
    return;
  }
```

- [ ] **Step 4: `characterData()` でルビテキスト収集モードを追加**

`characterData()` 内のスキップチェック（行823-825）の直後に追加:

```cpp
  // Collect ruby text instead of normal word processing
  if (self->collectingRubyText) {
    self->rubyTextBuffer.append(s, len);
    return;
  }
```

- [ ] **Step 5: `endElement()` にruby/rt閉じタグの処理を追加**

`endElement()` 内、depth デクリメント（行1078）の直後、skip終了チェック（行1095）の前に追加:

```cpp
  // Ruby text: </rt> closes collection, </ruby> distributes ruby to base words
  if (strcmp(name, "rt") == 0) {
    self->collectingRubyText = false;
  }
  if (strcmp(name, "ruby") == 0 && self->inRuby && self->currentTextBlock) {
    // Distribute ruby text evenly across base words
    const int currentWordCount = static_cast<int>(self->currentTextBlock->size());
    const int baseWordCount = currentWordCount - self->rubyStartWordIndex;
    if (baseWordCount > 0 && !self->rubyTextBuffer.empty()) {
      // Count UTF-8 characters in ruby text
      std::vector<size_t> charOffsets;
      const char* p = self->rubyTextBuffer.c_str();
      while (*p) {
        charOffsets.push_back(p - self->rubyTextBuffer.c_str());
        // Advance to next UTF-8 character
        if ((*p & 0x80) == 0) p += 1;
        else if ((*p & 0xE0) == 0xC0) p += 2;
        else if ((*p & 0xF0) == 0xE0) p += 3;
        else p += 4;
      }
      charOffsets.push_back(self->rubyTextBuffer.size());  // end sentinel
      const int rubyCharCount = static_cast<int>(charOffsets.size() - 1);

      for (int i = 0; i < baseWordCount; i++) {
        const int start = i * rubyCharCount / baseWordCount;
        const int end = (i + 1) * rubyCharCount / baseWordCount;
        if (start < end) {
          std::string portion = self->rubyTextBuffer.substr(
              charOffsets[start], charOffsets[end] - charOffsets[start]);
          self->currentTextBlock->setRubyForWordAt(self->rubyStartWordIndex + i, portion);
        }
      }
    }
    self->inRuby = false;
    self->rubyStartWordIndex = -1;
    self->rubyTextBuffer.clear();
  }
```

- [ ] **Step 6: ParsedText に `setRubyForWordAt` メソッドを追加**

`lib/Epub/Epub/ParsedText.h` の public に追加:

```cpp
  void setRubyForWordAt(size_t index, const std::string& ruby);
```

`lib/Epub/Epub/ParsedText.cpp` に実装:

```cpp
void ParsedText::setRubyForWordAt(size_t index, const std::string& ruby) {
  if (index < rubyTexts.size()) {
    rubyTexts[index] = ruby;
  }
}
```

- [ ] **Step 7: ビルド確認**

Run: `pio run 2>&1 | tail -5`
Expected: SUCCESS

- [ ] **Step 8: コミット**

```bash
git add lib/Epub/Epub/parsers/ChapterHtmlSlimParser.h lib/Epub/Epub/parsers/ChapterHtmlSlimParser.cpp lib/Epub/Epub/ParsedText.h lib/Epub/Epub/ParsedText.cpp
git commit -m "✨ HTMLパーサーでruby/rtタグをパースしルビテキストを収集"
```

---

### Task 4: ルビ行の行高さ・列幅調整

**Files:**
- Modify: `lib/Epub/Epub/parsers/ChapterHtmlSlimParser.cpp:1254-1314`

- [ ] **Step 1: `addLineToPage()` でルビ付き行の高さを調整**

`lib/Epub/Epub/parsers/ChapterHtmlSlimParser.cpp` の `addLineToPage()` (行1254-1314)。

横書きモード（行1288-1313）のlineHeight計算を変更:

```cpp
// 行1256 を変更:
// 変更前:
  const int lineHeight = renderer.getLineHeight(effectiveFontId) * lineCompression;

// 変更後:
  int lineHeight = renderer.getLineHeight(effectiveFontId) * lineCompression;
  if (line->hasRuby()) {
    lineHeight += renderer.getLineHeight(SMALL_FONT_ID) + 2;  // ルビフォント高さ + gap
  }
```

縦書きモード（行1258-1287）のcolumnWidth計算を変更:

```cpp
// 行1260-1261 を変更:
// 変更前:
    const int columnWidth = lineHeight;  // column width = line height (character cell width)
    const int columnSpacing = columnWidth / 4;

// 変更後:
    int columnWidth = renderer.getLineHeight(effectiveFontId) * lineCompression;
    if (line->hasRuby()) {
      columnWidth += renderer.getLineHeight(SMALL_FONT_ID) + 2;
    }
    const int columnSpacing = columnWidth / 4;
```

`fontIds.h` のインクルードを追加（ファイル先頭のインクルード群に）:

```cpp
#include "fontIds.h"
```

- [ ] **Step 2: ビルド確認**

Run: `pio run 2>&1 | tail -5`
Expected: SUCCESS

- [ ] **Step 3: コミット**

```bash
git add lib/Epub/Epub/parsers/ChapterHtmlSlimParser.cpp
git commit -m "👍 ルビ付き行の行高さ・列幅を自動調整"
```

---

### Task 5: セクションキャッシュバージョンのインクリメント

**Files:**
- Modify: `lib/Epub/Epub/Section.cpp:15`

- [ ] **Step 1: SECTION_FILE_VERSION を +1**

`lib/Epub/Epub/Section.cpp` 行15:

```cpp
// 変更前:
constexpr uint8_t SECTION_FILE_VERSION = 30;

// 変更後:
constexpr uint8_t SECTION_FILE_VERSION = 31;
```

- [ ] **Step 2: ビルド確認**

Run: `pio run 2>&1 | tail -5`
Expected: SUCCESS

- [ ] **Step 3: コミット**

```bash
git add lib/Epub/Epub/Section.cpp
git commit -m "👍 セクションキャッシュバージョンを31に更新（ルビデータ追加に伴う）"
```

---

### Task 6: クリーンビルド検証

- [ ] **Step 1: クリーンビルド実行**

Run: `pio run -t clean && pio run 2>&1 | tail -20`
Expected: SUCCESS

- [ ] **Step 2: RAM/Flash使用量確認**

RAM増加: `rubyTexts` ベクタのヘッダー（24バイト/TextBlock）のみ。ルビなし書籍では実質増加なし。

- [ ] **Step 3: 実機テスト手順（人間が実施）**

1. ルビ付きEPUB（例: 青空文庫の芥川龍之介作品）を開く
2. 横書き: ルビが親文字の上に小フォントで表示されることを確認
3. 縦書き: ルビが親文字の右側に表示されることを確認
4. 改行位置でルビが正しく分割されていることを確認
5. ルビのない書籍を開き、レイアウトが従来と変わらないことを確認（回帰テスト）
6. 旧キャッシュのEPUBを開き、自動再生成されることを確認

## Done 判定基準

- [ ] クリーンビルド成功（`pio run -t clean && pio run`）
- [ ] ルビ付きEPUBで振り仮名が正しく表示される（横書き + 縦書き）
- [ ] 改行位置でルビが分断されない
- [ ] ルビなし書籍のレイアウトに回帰がない
- [ ] 旧キャッシュが自動再生成される
- [ ] `git diff` で意図しない変更がないこと

※ 必須ゲート（動作検証・既存機能・差分確認・シークレット）は常に適用
